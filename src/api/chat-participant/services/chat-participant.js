'use strict';

/**
 * chat-participant service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::chat-participant.chat-participant', ({ strapi }) => ({
  // Custom service methods
}));
